# Changelog

## 0.1.0

Initial v0.1 release:
- CoordCard v0.1 JSON Schema
- Minimal + strict + multicycle example payloads
- TypeScript reference implementation (validate, assistive scoring, deterministic nextStep, CLI)
- Rule traceability (`why.ruleSource` / `why.ruleText`)
- Escalation + decay (prevents permanent repair lock)
